spring:
  datasource:
    url: jdbc:h2:mem:testing_test;DB_CLOSE_DELAY=-1;MODE=MySQL
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: WARN
    com.example: INFO
